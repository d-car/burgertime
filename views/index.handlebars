<div class="container">
  <div class="row">
    <div class="center col-sm-12">
      <h1>Burger Time</h1><br>
      <h2>Take a look at our current menu. If you don't see anything you like, add it yourself and munch!</h2>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="center col-sm-12">
      <h3>Menu</h3>
    </div>
  </div>

  <div class="row">
    <div class="center col-sm-6">  
      <ul>
        {{#each burgers}}
          {{#unless devoured}}
            {{> burgers/burgers-block devour=true}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="center col-sm-6">
      <ul>
      {{#each burgers}}
        {{#if devoured}}
          {{> burgers/burgers-block devour=false}}
        {{/if}}
      {{/each}}
      </ul>
    </div>
  </div>
</div>
  <h3>Whaddaya Want?</h3>
  <form class="create-form">

    <div class="form-group">
      <label for="ca">Your Burger Desire:</label>
      <input type="text" id="bName" name="name">
    </div>
    <button type="button" class="btn btn-success">Add Burger to Menu!</button>
  </form>
</div>
